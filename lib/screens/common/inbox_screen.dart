import 'package:flutter/material.dart';

class InboxScreen extends StatelessWidget {
  static const routeName = 'inbox-screen';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ibox Page'),
    );
  }
}
