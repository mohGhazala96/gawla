import 'package:flutter/material.dart';

class AddTourScreen extends StatelessWidget {
  static const routeName = 'add-tour-screen';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('add tour Page'),
    );
  }
}
