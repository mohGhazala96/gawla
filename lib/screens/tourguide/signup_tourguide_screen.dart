import 'package:flutter/material.dart';

class SignUpTourGuideScreen extends StatelessWidget {
  static const routeName = 'signup_tourguide_screen';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Signup TourGuide  Page'),
    );
  }
}
